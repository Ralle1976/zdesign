     1→     1→'use client';
     2→     2→
     3→     3→import { useState, useRef, useCallback, useEffect } from 'react';
     4→     4→import { useZDesignStore } from '@/stores/zdesign-store';
     5→     5→import { useI18n } from '@/i18n';
     6→     6→import { useVoiceInput } from '@/hooks/useVoiceInput';
     7→     7→import type { ChatMessage, DesignQualityReport } from '@/types/design';
     8→     8→import { ScrollArea } from '@/components/ui/scroll-area';
     9→     9→import { Button } from '@/components/ui/button';
    10→    10→import { Textarea } from '@/components/ui/textarea';
    11→    11→import { Avatar, AvatarFallback } from '@/components/ui/avatar';
    12→    12→import { Badge } from '@/components/ui/badge';
    13→    13→import { Separator } from '@/components/ui/separator';
    14→    14→import { Progress } from '@/components/ui/progress';
    15→    15→import {
    16→    16→  Tooltip,
    17→    17→  TooltipContent,
    18→    18→  TooltipTrigger,
    19→    19→} from '@/components/ui/tooltip';
    20→    20→import {
    21→    21→  Send,
    22→    22→  Mic,
    23→    23→  Paperclip,
    24→    24→  Trash2,
    25→    25→  Sparkles,
    26→    26→  LayoutDashboard,
    27→    27→  Smartphone,
    28→    28→  Presentation,
    29→    29→  Bot,
    30→    30→  User,
    31→    31→  ImageIcon,
    32→    32→  Search,
    33→    33→  CheckCircle2,
    34→    34→  Brain,
    35→    35→  Palette,
    36→    36→  Zap,
    37→    37→  Loader2,
    38→    38→  Wand2,
    39→    39→} from 'lucide-react';
    40→    40→import ReactMarkdown from 'react-markdown';
    41→    41→import { motion, AnimatePresence } from 'framer-motion';
    42→    42→import { AIImageDialog } from './AIImageDialog';
    43→    43→import { ResearchDialog } from './ResearchDialog';
    44→    44→
    45→    45→const EXAMPLE_PROMPTS = [
    46→    46→  {
    47→    47→    icon: LayoutDashboard,
    48→    48→    key: 'landing' as const,
    49→    49→    gradient: 'from-emerald-500 to-teal-500',
    50→    50→  },
    51→    51→  {
    52→    52→    icon: LayoutDashboard,
    53→    53→    key: 'dashboard' as const,
    54→    54→    gradient: 'from-teal-500 to-cyan-500',
    55→    55→  },
    56→    56→  {
    57→    57→    icon: Smartphone,
    58→    58→    key: 'app' as const,
    59→    59→    gradient: 'from-cyan-500 to-emerald-500',
    60→    60→  },
    61→    61→  {
    62→    62→    icon: Presentation,
    63→    63→    key: 'slide' as const,
    64→    64→    gradient: 'from-emerald-500 to-green-500',
    65→    65→  },
    66→    66→];
    67→    67→
    68→    68→// ============ Step Progress in Chat ============
    69→    69→
    70→    70→function GenerationStepProgress() {
    71→    71→  const { locale } = useI18n();
    72→    72→  const generationProgress = useZDesignStore((s) => s.generationProgress);
    73→    73→  const creativeMode = useZDesignStore((s) => s.creativeMode);
    74→    74→  const isDe = locale === 'de';
    75→    75→
    76→    76→  const steps = [
    77→    77→    { id: 'analyzing', label: isDe ? 'Analysiere...' : 'Analyzing request...', icon: Brain },
    78→    78→    { id: 'generating', label: isDe ? 'Generiere Layout...' : 'Generating layout...', icon: Sparkles },
    79→    79→    { id: 'rendering', label: isDe ? 'Füge Stile hinzu...' : 'Adding styles...', icon: Palette },
    80→    80→    { id: 'evaluating', label: isDe ? 'Finalisiere...' : 'Finalizing design...', icon: CheckCircle2 },
    81→    81→  ];
    82→    82→
    83→    83→  const currentIdx = steps.findIndex((s) => s.id === generationProgress.stage);
    84→    84→
    85→    85→  return (
    86→    86→    <div className="px-4 py-2 space-y-2">
    87→    87→      {/* Main status line */}
    88→    88→      <div className="flex items-center gap-2">
    89→    89→        <Loader2 className="size-3.5 animate-spin text-emerald-500 shrink-0" />
    90→    90→        <span className="text-xs font-medium text-foreground">
    91→    91→          {isDe ? 'KI generiert Design...' : 'Generating design with AI...'}
    92→    92→        </span>
    93→    93→        {generationProgress.estimatedTimeLeft && generationProgress.estimatedTimeLeft > 0 && (
    94→    94→          <span className="text-[10px] text-muted-foreground ml-auto">
    95→    95→            ~{generationProgress.estimatedTimeLeft}s {isDe ? 'übrig' : 'remaining'}
    96→    96→          </span>
    97→    97→        )}
    98→    98→      </div>
    99→    99→
   100→   100→      {/* Progress bar */}
   101→   101→      <Progress value={generationProgress.percentage} className="h-1.5 [&>div]:bg-emerald-500" />
   102→   102→
   103→   103→      {/* Steps */}
   104→   104→      <div className="flex items-center gap-1">
   105→   105→        {steps.map((step, idx) => {
   106→   106→          const StepIcon = step.icon;
   107→   107→          const isActive = step.id === generationProgress.stage;
   108→   108→          const isDone = idx < currentIdx || generationProgress.stage === 'complete';
   109→   109→
   110→   110→          return (
   111→   111→            <div key={step.id} className="flex items-center gap-1 flex-1 min-w-0">
   112→   112→              <div className={`flex items-center gap-1 px-1.5 py-0.5 rounded text-[10px] font-medium truncate ${
   113→   113→                isActive
   114→   114→                  ? 'bg-emerald-100 text-emerald-700 dark:bg-emerald-900/30 dark:text-emerald-400'
   115→   115→                  : isDone
   116→   116→                    ? 'bg-emerald-50 text-emerald-600 dark:bg-emerald-900/20 dark:text-emerald-500'
   117→   117→                    : 'bg-muted/50 text-muted-foreground'
   118→   118→              }`}>
   119→   119→                {isActive ? (
   120→   120→                  <Loader2 className="size-2.5 animate-spin shrink-0" />
   121→   121→                ) : isDone ? (
   122→   122→                  <CheckCircle2 className="size-2.5 shrink-0" />
   123→   123→                ) : (
   124→   124→                  <StepIcon className="size-2.5 shrink-0" />
   125→   125→                )}
   126→   126→                <span className="truncate">{step.label}</span>
   127→   127→              </div>
   128→   128→              {idx < steps.length - 1 && (
   129→   129→                <div className={`h-px flex-1 min-w-[8px] ${isDone ? 'bg-emerald-300 dark:bg-emerald-700' : 'bg-border'}`} />
   130→   130→              )}
   131→   131→            </div>
   132→   132→          );
   133→   133→        })}
   134→   134→      </div>
   135→   135→
   136→   136→      {/* Creative mode indicator */}
   137→   137→      {creativeMode && (
   138→   138→        <div className="flex items-center gap-1 text-[10px] text-amber-600 dark:text-amber-400">
   139→   139→          <Zap className="size-2.5" />
   140→   140→          <span className="font-medium">Creative Mode active</span>
   141→   141→        </div>
   142→   142→      )}
   143→   143→
   144→   144→      {/* Status message */}
   145→   145→      {generationProgress.message && (
   146→   146→        <p className="text-[10px] text-muted-foreground">{generationProgress.message}</p>
   147→   147→      )}
   148→   148→    </div>
   149→   149→  );
   150→   150→}
   151→   151→
   152→   152→function TypingIndicator() {
   153→   153→  return (
   154→   154→    <div className="flex items-start gap-3 px-4 py-3">
   155→   155→      <Avatar className="size-8 shrink-0">
   156→   156→        <AvatarFallback className="bg-gradient-to-br from-emerald-500 to-teal-600 text-white text-xs font-bold">
   157→   157→          Z
   158→   158→        </AvatarFallback>
   159→   159→      </Avatar>
   160→   160→      <div className="flex items-center gap-1 bg-muted rounded-2xl rounded-tl-sm px-4 py-2.5">
   161→   161→        <motion.span
   162→   162→          className="size-2 rounded-full bg-emerald-500"
   163→   163→          animate={{ opacity: [0.4, 1, 0.4] }}
   164→   164→          transition={{ duration: 1.2, repeat: Infinity, delay: 0 }}
   165→   165→        />
   166→   166→        <motion.span
   167→   167→          className="size-2 rounded-full bg-emerald-500"
   168→   168→          animate={{ opacity: [0.4, 1, 0.4] }}
   169→   169→          transition={{ duration: 1.2, repeat: Infinity, delay: 0.2 }}
   170→   170→        />
   171→   171→        <motion.span
   172→   172→          className="size-2 rounded-full bg-emerald-500"
   173→   173→          animate={{ opacity: [0.4, 1, 0.4] }}
   174→   174→          transition={{ duration: 1.2, repeat: Infinity, delay: 0.4 }}
   175→   175→        />
   176→   176→      </div>
   177→   177→    </div>
   178→   178→  );
   179→   179→}
   180→   180→
   181→   181→function MessageBubble({ message, qualityReport, onEnhance, isEnhancing }: {
   182→   182→  message: ChatMessage;
   183→   183→  qualityReport: DesignQualityReport | null;
   184→   184→  onEnhance: () => void;
   185→   185→  isEnhancing: boolean;
   186→   186→}) {
   187→   187→  // Hydration-safe: only render timestamp after mount
   188→   188→  const [mounted, setMounted] = useState(false);
   189→   189→  useEffect(() => { setMounted(true); }, []); // eslint-disable-line react-hooks/set-state-in-effect
   190→   190→
   191→   191→  if (message.role === 'system') {
   192→   192→    return (
   193→   193→      <div className="flex justify-center px-4 py-1">
   194→   194→        <span className="text-xs text-muted-foreground bg-muted/50 px-3 py-1 rounded-full">
   195→   195→          {message.content}
   196→   196→        </span>
   197→   197→      </div>
   198→   198→    );
   199→   199→  }
   200→   200→
   201→   201→  const isUser = message.role === 'user';
   202→   202→  const hasDesign = message.role === 'assistant' && message.metadata?.designUpdate;
   203→   203→  const showQuality = hasDesign && qualityReport;
   204→   204→  const isTemplateUsed = message.role === 'assistant' && message.metadata?.templateUsed;
   205→   205→
   206→   206→  return (
   207→   207→    <motion.div
   208→   208→      initial={{ opacity: 0, y: 8 }}
   209→   209→      animate={{ opacity: 1, y: 0 }}
   210→   210→      transition={{ duration: 0.25, ease: 'easeOut' }}
   211→   211→      className={`flex items-start gap-3 px-4 py-3 ${isUser ? 'flex-row-reverse' : ''}`}
   212→   212→    >
   213→   213→      <Avatar className="size-8 shrink-0">
   214→   214→        <AvatarFallback
   215→   215→          className={
   216→   216→            isUser
   217→   217→              ? 'bg-secondary text-secondary-foreground text-xs'
   218→   218→              : 'bg-gradient-to-br from-emerald-500 to-teal-600 text-white text-xs font-bold'
   219→   219→          }
   220→   220→        >
   221→   221→          {isUser ? <User className="size-4" /> : <Bot className="size-4" />}
   222→   222→        </AvatarFallback>
   223→   223→      </Avatar>
   224→   224→
   225→   225→      <div
   226→   226→        className={`max-w-[85%] space-y-1 ${isUser ? 'items-end' : 'items-start'}`}
   227→   227→      >
   228→   228→        <div
   229→   229→          className={`rounded-2xl px-4 py-2.5 text-sm leading-relaxed ${
   230→   230→            isUser
   231→   231→              ? 'bg-emerald-600 text-white rounded-tr-sm'
   232→   232→              : 'bg-muted rounded-tl-sm'
   233→   233→          }`}
   234→   234→        >
   235→   235→          {isUser ? (
   236→   236→            <p className="whitespace-pre-wrap">{message.content}</p>
   237→   237→          ) : (
   238→   238→            <div className="prose prose-sm dark:prose-invert max-w-none [&_p]:my-1 [&_ul]:my-1 [&_ol]:my-1 [&_li]:my-0.5 [&_code]:text-xs [&_code]:bg-black/10 [&_code]:px-1 [&_code]:py-0.5 [&_code]:rounded [&_pre]:bg-black/5 [&_pre]:rounded-md [&_pre]:p-2">
   239→   239→              <ReactMarkdown>{message.content}</ReactMarkdown>
   240→   240→            </div>
   241→   241→          )}
   242→   242→        </div>
   243→   243→        {/* Quality Score Badge for design messages */}
   244→   244→        {showQuality && (
   245→   245→          <motion.div
   246→   246→            initial={{ opacity: 0, y: 4 }}
   247→   247→            animate={{ opacity: 1, y: 0 }}
   248→   248→            transition={{ delay: 0.2 }}
   249→   249→            className="flex items-center gap-2 px-1"
   250→   250→          >
   251→   251→            <div className={`flex items-center gap-1.5 px-2.5 py-1 rounded-lg text-xs font-medium ${
   252→   252→              qualityReport.overallScore >= 80
   253→   253→                ? 'bg-emerald-100 text-emerald-700 dark:bg-emerald-900/30 dark:text-emerald-400'
   254→   254→                : qualityReport.overallScore >= 60
   255→   255→                  ? 'bg-amber-100 text-amber-700 dark:bg-amber-900/30 dark:text-amber-400'
   256→   256→                  : 'bg-red-100 text-red-700 dark:bg-red-900/30 dark:text-red-400'
   257→   257→            }`}>
   258→   258→              <CheckCircle2 className="size-3" />
   259→   259→              Quality: {qualityReport.overallScore}/100
   260→   260→              <div className="flex gap-0.5 ml-1">
   261→   261→                {[
   262→   262→                  { label: 'CSS', value: qualityReport.cssValid },
   263→   263→                  { label: 'Sem', value: qualityReport.semantics },
   264→   264→                  { label: 'Rsp', value: qualityReport.responsiveness },
   265→   265→                  { label: 'A11y', value: qualityReport.accessibility },
   266→   266→                ].map((s) => (
   267→   267→                  <span key={s.label} className={`text-[9px] px-1 rounded ${
   268→   268→                    s.value >= 80 ? 'bg-emerald-200/50 dark:bg-emerald-800/30' : s.value >= 60 ? 'bg-amber-200/50 dark:bg-amber-800/30' : 'bg-red-200/50 dark:bg-red-800/30'
   269→   269→                  }`}>
   270→   270→                    {s.label}:{s.value}
   271→   271→                  </span>
   272→   272→                ))}
   273→   273→              </div>
   274→   274→            </div>
   275→   275→            {/* Enhance button when quality < 80 */}
   276→   276→            {qualityReport.overallScore < 80 && (
   277→   277→              <Button
   278→   278→                size="sm"
   279→   279→                variant="outline"
   280→   280→                className="h-7 text-[11px] px-2.5 gap-1 border-amber-300 text-amber-700 hover:bg-amber-50 dark:border-amber-800 dark:text-amber-400 dark:hover:bg-amber-900/20"
   281→   281→                onClick={onEnhance}
   282→   282→                disabled={isEnhancing}
   283→   283→              >
   284→   284→                {isEnhancing ? (
   285→   285→                  <Loader2 className="size-3 animate-spin" />
   286→   286→                ) : (
   287→   287→                  <Wand2 className="size-3" />
   288→   288→                )}
   289→   289→                Enhance
   290→   290→              </Button>
   291→   291→            )}
   292→   292→          </motion.div>
   293→   293→        )}
   294→   294→        {/* AI-guided template indicator */}
   295→   295→        {isTemplateUsed && (
   296→   296→          <motion.div
   297→   297→            initial={{ opacity: 0, y: 4 }}
   298→   298→            animate={{ opacity: 1, y: 0 }}
   299→   299→            transition={{ delay: 0.3 }}
   300→   300→            className="flex items-center gap-1.5 px-1"
   301→   301→          >
   302→   302→            <span className="text-[11px] text-sky-600 dark:text-sky-400 flex items-center gap-1">
   303→   303→              <Sparkles className="size-3" />
   304→   304→              AI-guided template — You can customize this design by telling me what to change!
   305→   305→            </span>
   306→   306→          </motion.div>
   307→   307→        )}
   308→   308→        {mounted && (
   309→   309→          <p
   310→   310→            className={`text-[10px] text-muted-foreground px-1 ${isUser ? 'text-right' : ''}`}
   311→   311→          >
   312→   312→            {new Date(message.createdAt).toLocaleTimeString([], {
   313→   313→              hour: '2-digit',
   314→   314→              minute: '2-digit',
   315→   315→            })}
   316→   316→          </p>
   317→   317→        )}
   318→   318→      </div>
   319→   319→    </motion.div>
   320→   320→  );
   321→   321→}
   322→   322→
   323→   323→function WelcomeState({
   324→   324→  onExampleClick,
   325→   325→}: {
   326→   326→  onExampleClick: (prompt: string) => void;
   327→   327→}) {
   328→   328→  const { t } = useI18n();
   329→   329→
   330→   330→  return (
   331→   331→    <div className="flex flex-col items-center justify-center h-full px-6 py-8">
   332→   332→      <motion.div
   333→   333→        initial={{ opacity: 0, scale: 0.95 }}
   334→   334→        animate={{ opacity: 1, scale: 1 }}
   335→   335→        transition={{ duration: 0.4, ease: 'easeOut' }}
   336→   336→        className="text-center space-y-4 max-w-sm"
   337→   337→      >
   338→   338→        {/* Logo */}
   339→   339→        <div className="flex items-center justify-center size-14 rounded-2xl bg-gradient-to-br from-emerald-500 to-teal-600 text-white font-bold text-2xl shadow-lg shadow-emerald-500/20 mx-auto">
   340→   340→          Z
   341→   341→        </div>
   342→   342→
   343→   343→        <div>
   344→   344→          <h2 className="text-lg font-semibold">{t.appName}</h2>
   345→   345→          <p className="text-sm text-muted-foreground mt-1 leading-relaxed">
   346→   346→            {t.chat.welcome}
   347→   347→          </p>
   348→   348→        </div>
   349→   349→
   350→   350→        {/* Example prompts */}
   351→   351→        <div className="grid grid-cols-1 gap-2 w-full mt-4">
   352→   352→          {EXAMPLE_PROMPTS.map((example) => (
   353→   353→            <motion.button
   354→   354→              key={example.key}
   355→   355→              initial={{ opacity: 0, y: 8 }}
   356→   356→              animate={{ opacity: 1, y: 0 }}
   357→   357→              transition={{ duration: 0.3, delay: 0.1 }}
   358→   358→              whileHover={{ scale: 1.02 }}
   359→   359→              whileTap={{ scale: 0.98 }}
   360→   360→              onClick={() => onExampleClick(t.chat.examples[example.key])}
   361→   361→              className="flex items-center gap-3 p-3 rounded-xl border bg-card hover:bg-accent/50 transition-colors text-left group"
   362→   362→            >
   363→   363→              <div
   364→   364→                className={`flex items-center justify-center size-9 rounded-lg bg-gradient-to-br ${example.gradient} text-white shrink-0`}
   365→   365→              >
   366→   366→                <example.icon className="size-4" />
   367→   367→              </div>
   368→   368→              <span className="text-sm text-muted-foreground group-hover:text-foreground transition-colors">
   369→   369→                {t.chat.examples[example.key]}
   370→   370→              </span>
   371→   371→            </motion.button>
   372→   372→          ))}
   373→   373→        </div>
   374→   374→      </motion.div>
   375→   375→    </div>
   376→   376→  );
   377→   377→}
   378→   378→
   379→   379→export function ChatPanel() {
   380→   380→  const { t } = useI18n();
   381→   381→  const chatMessages = useZDesignStore((s) => s.chatMessages);
   382→   382→  const addChatMessage = useZDesignStore((s) => s.addChatMessage);
   383→   383→  const clearChat = useZDesignStore((s) => s.clearChat);
   384→   384→  const isGenerating = useZDesignStore((s) => s.isGenerating);
   385→   385→  const setIsGenerating = useZDesignStore((s) => s.setIsGenerating);
   386→   386→  const projectId = useZDesignStore((s) => s.projectId);
   387→   387→  const designTree = useZDesignStore((s) => s.designTree);
   388→   388→  const designSystem = useZDesignStore((s) => s.designSystem);
   389→   389→  const setDesignTree = useZDesignStore((s) => s.setDesignTree);
   390→   390→  const setGenerationProgress = useZDesignStore((s) => s.setGenerationProgress);
   391→   391→  const resetGenerationProgress = useZDesignStore((s) => s.resetGenerationProgress);
   392→   392→  const setQualityReport = useZDesignStore((s) => s.setQualityReport);
   393→   393→  const qualityReport = useZDesignStore((s) => s.qualityReport);
   394→   394→  const generationProgress = useZDesignStore((s) => s.generationProgress);
   395→   395→
   396→   396→  const [input, setInput] = useState('');
   397→   397→  const [isEnhancing, setIsEnhancing] = useState(false);
   398→   398→  const scrollRef = useRef<HTMLDivElement>(null);
   399→   399→  const textareaRef = useRef<HTMLTextAreaElement>(null);
   400→   400→
   401→   401→  // AI Image dialog
   402→   402→  const [aiImageOpen, setAiImageOpen] = useState(false);
   403→   403→
   404→   404→  // Research dialog
   405→   405→  const [researchOpen, setResearchOpen] = useState(false);
   406→   406→
   407→   407→  // === Refs for mutable state to avoid stale closures ===
   408→   408→  const projectIdRef = useRef(projectId);
   409→   409→  const isGeneratingRef = useRef(isGenerating);
   410→   410→  const chatMessagesRef = useRef(chatMessages);
   411→   411→  const designTreeRef = useRef(designTree);
   412→   412→  const designSystemRef = useRef(designSystem);
   413→   413→
   414→   414→  useEffect(() => { projectIdRef.current = projectId; }, [projectId]);
   415→   415→  useEffect(() => { isGeneratingRef.current = isGenerating; }, [isGenerating]);
   416→   416→  useEffect(() => { chatMessagesRef.current = chatMessages; }, [chatMessages]);
   417→   417→  useEffect(() => { designTreeRef.current = designTree; }, [designTree]);
   418→   418→  useEffect(() => { designSystemRef.current = designSystem; }, [designSystem]);
   419→   419→
   420→   420→  // Stable store action refs
   421→   421→  const addChatMessageRef = useRef(addChatMessage);
   422→   422→  const setIsGeneratingRef = useRef(setIsGenerating);
   423→   423→  const setDesignTreeRef = useRef(setDesignTree);
   424→   424→  const setGenerationProgressRef = useRef(setGenerationProgress);
   425→   425→  const resetGenerationProgressRef = useRef(resetGenerationProgress);
   426→   426→  const setQualityReportRef = useRef(setQualityReport);
   427→   427→  const tRef = useRef(t);
   428→   428→
   429→   429→  useEffect(() => { addChatMessageRef.current = addChatMessage; }, [addChatMessage]);
   430→   430→  useEffect(() => { setIsGeneratingRef.current = setIsGenerating; }, [setIsGenerating]);
   431→   431→  useEffect(() => { setDesignTreeRef.current = setDesignTree; }, [setDesignTree]);
   432→   432→  useEffect(() => { setGenerationProgressRef.current = setGenerationProgress; }, [setGenerationProgress]);
   433→   433→  useEffect(() => { resetGenerationProgressRef.current = resetGenerationProgress; }, [resetGenerationProgress]);
   434→   434→  useEffect(() => { setQualityReportRef.current = setQualityReport; }, [setQualityReport]);
   435→   435→  useEffect(() => { tRef.current = t; }, [t]);
   436→   436→
   437→   437→  // ============ Progress simulation ============
   438→   438→  // Simulates step-by-step progress while the API call is in flight
   439→   439→  // LLM takes ~60-90s, so we simulate progress over that duration
   440→   440→  const progressTimerRef = useRef<ReturnType<typeof setInterval> | null>(null);
   441→   441→
   442→   442→  const startProgressSimulation = useCallback(() => {
   443→   443→    // Only set up incremental percentage updates during the API call
   444→   444→    // Initial stage is already set by sendMessage
   445→   445→    const startTime = Date.now();
   446→   446→    const totalDuration = 80000; // 80 seconds estimated total for LLM generation
   447→   447→
   448→   448→    // Auto-advance stages on a timer
   449→   449→    // analyzing: 0-5s, generating: 5-55s, rendering: handled after API returns
   450→   450→    const stageTimers: Array<{ delay: number; stage: string; pct: number; msg: string }> = [
   451→   451→      { delay: 5000, stage: 'generating', pct: 25, msg: 'AI is creating your design...' },
   452→   452→      { delay: 20000, stage: 'generating', pct: 35, msg: 'Building layout structure...' },
   453→   453→      { delay: 40000, stage: 'generating', pct: 50, msg: 'Adding components and styles...' },
   454→   454→      { delay: 60000, stage: 'generating', pct: 60, msg: 'Polishing design details...' },
   455→   455→    ];
   456→   456→
   457→   457→    const stageTimeouts: Array<ReturnType<typeof setTimeout>> = [];
   458→   458→    for (const st of stageTimers) {
   459→   459→      stageTimeouts.push(setTimeout(() => {
   460→   460→        setGenerationProgressRef.current({
   461→   461→          stage: st.stage as 'analyzing' | 'generating' | 'rendering' | 'evaluating' | 'complete' | 'error' | 'idle',
   462→   462→          stageLabel: st.msg,
   463→   463→          percentage: st.pct,
   464→   464→          message: st.msg,
   465→   465→          estimatedTimeLeft: Math.max(0, Math.round((totalDuration - st.delay) / 1000)),
   466→   466→        });
   467→   467→      }, st.delay));
   468→   468→    }
   469→   469→
   470→   470→    progressTimerRef.current = setInterval(() => {
   471→   471→      const elapsed = Date.now() - startTime;
   472→   472→      const pct = Math.min(65, Math.round(20 + (elapsed / totalDuration) * 45)); // 20-65% range
   473→   473→      const timeLeft = Math.max(0, Math.round((totalDuration - elapsed) / 1000));
   474→   474→
   475→   475→      // Update percentage and time remaining
   476→   476→      setGenerationProgressRef.current({
   477→   477→        percentage: pct,
   478→   478→        estimatedTimeLeft: timeLeft,
   479→   479→      });
   480→   480→    }, 1000);
   481→   481→
   482→   482→    // Store stage timeouts for cleanup
   483→   483→    (progressTimerRef as React.MutableRefObject<ReturnType<typeof setInterval> | null & { stageTimeouts?: Array<ReturnType<typeof setTimeout>> }>).current = {
   484→   484→      ...progressTimerRef.current,
   485→   485→      stageTimeouts,
   486→   486→    } as unknown as ReturnType<typeof setInterval>;
   487→   487→  }, []);
   488→   488→
   489→   489→  const stopProgressSimulation = useCallback((success: boolean) => {
   490→   490→    if (progressTimerRef.current) {
   491→   491→      clearInterval(progressTimerRef.current);
   492→   492→      // Clean up stage timeouts
   493→   493→      const ref = progressTimerRef.current as unknown as { stageTimeouts?: Array<ReturnType<typeof setTimeout>> };
   494→   494→      if (ref.stageTimeouts) {
   495→   495→        ref.stageTimeouts.forEach(clearTimeout);
   496→   496→      }
   497→   497→      progressTimerRef.current = null;
   498→   498→    }
   499→   499→
   500→   500→    if (success) {
   501→   501→      // If we're still in evaluating stage from sendMessage, set complete
   502→   502→      // If we haven't reached evaluating yet, skip to complete
   503→   503→      setGenerationProgressRef.current({
   504→   504→        stage: 'complete',
   505→   505→        stageLabel: 'Complete!',
   506→   506→        percentage: 100,
   507→   507→        message: 'Design generated successfully!',
   508→   508→      });
   509→   509→
   510→   510→      // Reset after a short delay
   511→   511→      setTimeout(() => {
   512→   512→        resetGenerationProgressRef.current();
   513→   513→      }, 1500);
   514→   514→    } else {
   515→   515→      setGenerationProgressRef.current({
   516→   516→        stage: 'error',
   517→   517→        stageLabel: 'Error',
   518→   518→        percentage: 100,
   519→   519→        message: 'Generation failed',
   520→   520→      });
   521→   521→
   522→   522→      setTimeout(() => {
   523→   523→        resetGenerationProgressRef.current();
   524→   524→      }, 3000);
   525→   525→    }
   526→   526→  }, []);
   527→   527→
   528→   528→  // ============ Evaluate Design Quality ============
   529→   529→  const evaluateDesignQuality = useCallback((designTree: Record<string, unknown>) => {
   530→   530→    const report = {
   531→   531→      overallScore: 0,
   532→   532→      completeness: 0,
   533→   533→      cssValid: 0,
   534→   534→      semantics: 0,
   535→   535→      responsiveness: 0,
   536→   536→      accessibility: 0,
   537→   537→      issues: [] as Array<{
   538→   538→        severity: 'critical' | 'warning' | 'info';
   539→   539→        category: 'css' | 'semantics' | 'responsiveness' | 'accessibility' | 'completeness';
   540→   540→        message: string;
   541→   541→        autoFixable: boolean;
   542→   542→      }>,
   543→   543→      suggestions: [] as string[],
   544→   544→    };
   545→   545→
   546→   546→    // Check completeness
   547→   547→    const children = (designTree as { children?: unknown[] }).children;
   548→   548→    if (children && children.length > 0) {
   549→   549→      report.completeness = Math.min(100, children.length * 20);
   550→   550→      // Check for essential sections
   551→   551→      const hasNav = children.some((c: unknown) => {
   552→   552→        const node = c as { type?: string; tag?: string };
   553→   553→        return node.type === 'nav' || node.tag === 'nav' || node.type === 'header';
   554→   554→      });
   555→   555→      const hasFooter = children.some((c: unknown) => {
   556→   556→        const node = c as { type?: string; tag?: string };
   557→   557→        return node.type === 'footer' || node.tag === 'footer';
   558→   558→      });
   559→   559→      const hasContent = children.some((c: unknown) => {
   560→   560→        const node = c as { type?: string; tag?: string };
   561→   561→        return node.type === 'section' || node.tag === 'section' || node.type === 'main';
   562→   562→      });
   563→   563→
   564→   564→      if (!hasNav) {
   565→   565→        report.issues.push({
   566→   566→          severity: 'warning',
   567→   567→          category: 'completeness',
   568→   568→          message: 'No navigation section found',
   569→   569→          autoFixable: false,
   570→   570→        });
   571→   571→      }
   572→   572→      if (!hasFooter) {
   573→   573→        report.issues.push({
   574→   574→          severity: 'info',
   575→   575→          category: 'completeness',
   576→   576→          message: 'No footer section found',
   577→   577→          autoFixable: false,
   578→   578→        });
   579→   579→      }
   580→   580→      if (!hasContent) {
   581→   581→        report.issues.push({
   582→   582→          severity: 'warning',
   583→   583→          category: 'completeness',
   584→   584→          message: 'No main content section found',
   585→   585→          autoFixable: false,
   586→   586→        });
   587→   587→      }
   588→   588→
   589→   589→      if (hasNav && hasFooter && hasContent) {
   590→   590→        report.completeness = 100;
   591→   591→      }
   592→   592→    } else {
   593→   593→      report.issues.push({
   594→   594→        severity: 'critical',
   595→   595→        category: 'completeness',
   596→   596→        message: 'Design tree has no children',
   597→   597→        autoFixable: false,
   598→   598→      });
   599→   599→    }
   600→   600→
   601→   601→    // Check CSS validity (basic check)
   602→   602→    const styleStr = JSON.stringify(designTree);
   603→   603→    const hasTailwindShorthand = /\d+xl|text-md|p-lg|gap-md/.test(styleStr);
   604→   604→    if (hasTailwindShorthand) {
   605→   605→      report.cssValid = 50;
   606→   606→      report.issues.push({
   607→   607→        severity: 'critical',
   608→   608→        category: 'css',
   609→   609→        message: 'Found Tailwind shorthand in CSS values — should use real CSS (px, rem, %)',
   610→   610→        autoFixable: true,
   611→   611→      });
   612→   612→    } else {
   613→   613→      report.cssValid = 95;
   614→   614→    }
   615→   615→
   616→   616→    // Check semantics
   617→   617→    const semanticTags = ['header', 'nav', 'main', 'section', 'footer', 'article', 'aside'];
   618→   618→    const usedTags = new Set<string>();
   619→   619→    const findTags = (node: unknown) => {
   620→   620→      const n = node as { tag?: string; children?: unknown[] };
   621→   621→      if (n.tag) usedTags.add(n.tag);
   622→   622→      if (n.children) n.children.forEach(findTags);
   623→   623→    };
   624→   624→    findTags(designTree);
   625→   625→    const semanticCount = semanticTags.filter((tag) => usedTags.has(tag)).length;
   626→   626→    report.semantics = Math.min(100, Math.round((semanticCount / 3) * 100)); // 3+ semantic tags = good
   627→   627→
   628→   628→    // Check responsiveness
   629→   629→    const hasFlexOrGrid = /flex|grid/.test(styleStr);
   630→   630→    const hasPercentOrRelative = /%|rem|vh|vw/.test(styleStr);
   631→   631→    report.responsiveness = hasFlexOrGrid ? (hasPercentOrRelative ? 90 : 70) : 40;
   632→   632→
   633→   633→    // Check accessibility basics
   634→   634→    const hasHeadings = /"heading"|"h1"|"h2"|"h3"/.test(styleStr);
   635→   635→    const hasButtons = /"button"/.test(styleStr);
   636→   636→    report.accessibility = hasHeadings ? (hasButtons ? 80 : 60) : 40;
   637→   637→    if (!hasHeadings) {
   638→   638→      report.issues.push({
   639→   639→        severity: 'warning',
   640→   640→        category: 'accessibility',
   641→   641→        message: 'No heading elements found — important for screen readers',
   642→   642→        autoFixable: false,
   643→   643→      });
   644→   644→    }
   645→   645→
   646→   646→    // Suggestions
   647→   647→    if (report.responsiveness < 80) {
   648→   648→      report.suggestions.push('Consider using flexbox/grid layouts with relative units for better responsiveness');
   649→   649→    }
   650→   650→    if (report.semantics < 80) {
   651→   651→      report.suggestions.push('Use semantic HTML tags (header, nav, main, section, footer) for better structure');
   652→   652→    }
   653→   653→    if (report.accessibility < 80) {
   654→   654→      report.suggestions.push('Add proper heading hierarchy and ARIA labels for accessibility');
   655→   655→    }
   656→   656→
   657→   657→    // Overall score
   658→   658→    report.overallScore = Math.round(
   659→   659→      (report.completeness * 0.25 +
   660→   660→        report.cssValid * 0.2 +
   661→   661→        report.semantics * 0.2 +
   662→   662→        report.responsiveness * 0.2 +
   663→   663→        report.accessibility * 0.15)
   664→   664→    );
   665→   665→
   666→   666→    return report;
   667→   667→  }, []);
   668→   668→
   669→   669→  // === Handle Enhance Design ===
   670→   670→  const handleEnhance = useCallback(async () => {
   671→   671→    const currentDesignTree = designTreeRef.current;
   672→   672→    const currentQualityReport = useZDesignStore.getState().qualityReport;
   673→   673→
   674→   674→    if (!currentDesignTree || !currentDesignTree.children || currentDesignTree.children.length === 0) return;
   675→   675→
   676→   676→    setIsEnhancing(true);
   677→   677→    try {
   678→   678→      const res = await fetch('/api/design/enhance', {
   679→   679→        method: 'POST',
   680→   680→        headers: { 'Content-Type': 'application/json' },
   681→   681→        body: JSON.stringify({
   682→   682→          designTree: currentDesignTree,
   683→   683→          qualityReport: currentQualityReport,
   684→   684→        }),
   685→   685→      });
   686→   686→
   687→   687→      if (res.ok) {
   688→   688→        const data = await res.json();
   689→   689→        if (data.designTree) {
   690→   690→          setDesignTreeRef.current(data.designTree);
   691→   691→        }
   692→   692→        if (data.qualityReport) {
   693→   693→          setQualityReportRef.current(data.qualityReport);
   694→   694→        }
   695→   695→        // Add assistant message about enhancement
   696→   696→        const enhanceMessage: ChatMessage = {
   697→   697→          id: `ai-enhance-${Date.now()}`,
   698→   698→          projectId: projectIdRef.current || '',
   699→   699→          role: 'assistant',
   700→   700→          content: `I've enhanced your design! ${data.enhancementsApplied?.length ? 'Applied fixes: ' + data.enhancementsApplied.join(', ') + '.' : 'The quality has been improved.'}`,
   701→   701→          metadata: data.designTree ? { designUpdate: data.designTree } : undefined,
   702→   702→          createdAt: new Date(),
   703→   703→        };
   704→   704→        addChatMessageRef.current(enhanceMessage);
   705→   705→      }
   706→   706→    } catch {
   707→   707→      // Enhancement failed silently
   708→   708→    } finally {
   709→   709→      setIsEnhancing(false);
   710→   710→    }
   711→   711→  }, []);
   712→   712→
   713→   713→  // === sendMessage with progress tracking ===
   714→   714→  const sendMessage = useCallback(
   715→   715→    async (messageText: string) => {
   716→   716→      const currentProjectId = projectIdRef.current;
   717→   717→      const currentIsGenerating = isGeneratingRef.current;
   718→   718→
   719→   719→      if (!messageText.trim() || !currentProjectId || currentIsGenerating) return;
   720→   720→
   721→   721→      // Clean up any voice indicators from the message
   722→   722→      const cleanText = messageText.replace(/🎤/g, '').trim();
   723→   723→      if (!cleanText) return;
   724→   724→
   725→   725→      const userMessage: ChatMessage = {
   726→   726→        id: `user-${Date.now()}`,
   727→   727→        projectId: currentProjectId,
   728→   728→        role: 'user',
   729→   729→        content: cleanText,
   730→   730→        createdAt: new Date(),
   731→   731→      };
   732→   732→
   733→   733→      addChatMessageRef.current(userMessage);
   734→   734→      setInput('');
   735→   735→      setIsGeneratingRef.current(true);
   736→   736→
   737→   737→      // Start progress — set analyzing stage explicitly
   738→   738→      const progressStartTime = Date.now();
   739→   739→      setGenerationProgressRef.current({
   740→   740→        stage: 'analyzing',
   741→   741→        stageLabel: 'Analyzing request...',
   742→   742→        percentage: 10,
   743→   743→        message: 'Understanding your design request...',
   744→   744→        startedAt: progressStartTime,
   745→   745→        estimatedTimeLeft: 75,
   746→   746→      });
   747→   747→
   748→   748→      // Small delay for UX
   749→   749→      await new Promise((r) => setTimeout(r, 300));
   750→   750→
   751→   751→      // Move to generating stage
   752→   752→      setGenerationProgressRef.current({
   753→   753→        stage: 'generating',
   754→   754→        stageLabel: 'Generating design...',
   755→   755→        percentage: 30,
   756→   756→        message: 'Creating your design...',
   757→   757→        startedAt: progressStartTime,
   758→   758→      });
   759→   759→
   760→   760→      // Start progress simulation for incremental updates during API call
   761→   761→      startProgressSimulation();
   762→   762→
   763→   763→      try {
   764→   764→        const currentMessages = chatMessagesRef.current;
   765→   765→        const history = currentMessages.slice(-10).map((m) => ({
   766→   766→          role: m.role as 'user' | 'assistant',
   767→   767→          content: m.content,
   768→   768→        }));
   769→   769→
   770→   770→        const currentDesignTree = designTreeRef.current;
   771→   771→        const currentDesignSystem = designSystemRef.current;
   772→   772→
   773→   773→        const res = await fetch('/api/chat', {
   774→   774→          method: 'POST',
   775→   775→          headers: { 'Content-Type': 'application/json' },
   776→   776→          body: JSON.stringify({
   777→   777→            message: cleanText,
   778→   778→            projectId: currentProjectId,
   779→   779→            designTree: currentDesignTree.children && currentDesignTree.children.length > 0 ? currentDesignTree : undefined,
   780→   780→            designSystem: currentDesignSystem || undefined,
   781→   781→            history,
   782→   782→          }),
   783→   783→        });
   784→   784→
   785→   785→        if (res.ok) {
   786→   786→          const data = await res.json();
   787→   787→          const aiMessage: ChatMessage = {
   788→   788→            id: data.id || `ai-${Date.now()}`,
   789→   789→            projectId: currentProjectId,
   790→   790→            role: 'assistant',
   791→   791→            content: data.message || 'I processed your request.',
   792→   792→            metadata: data.design
   793→   793→              ? { designUpdate: data.design, usedFallback: data.usedFallback || false, templateUsed: data.templateUsed || false }
   794→   794→              : undefined,
   795→   795→            createdAt: new Date(data.createdAt || Date.now()),
   796→   796→          };
   797→   797→          addChatMessageRef.current(aiMessage);
   798→   798→
   799→   799→          // Show fallback info if the AI was unavailable (positive framing)
   800→   800→          if (data.usedFallback && !data.templateUsed) {
   801→   801→            const infoMessage: ChatMessage = {
   802→   802→              id: `fallback-info-${Date.now()}`,
   803→   803→              projectId: currentProjectId,
   804→   804→              role: 'system',
   805→   805→              content: '✨ AI-guided template used — You can customize this design by telling me what to change!',
   806→   806→              createdAt: new Date(),
   807→   807→            };
   808→   808→            addChatMessageRef.current(infoMessage);
   809→   809→          }
   810→   810→
   811→   811→          if (data.design) {
   812→   812→            setDesignTreeRef.current(data.design);
   813→   813→
   814→   814→            // Move to rendering stage
   815→   815→            setGenerationProgressRef.current({
   816→   816→              stage: 'rendering',
   817→   817→              stageLabel: 'Rendering preview...',
   818→   818→              percentage: 70,
   819→   819→              message: 'Preparing design preview...',
   820→   820→            });
   821→   821→
   822→   822→            // Small delay for UX
   823→   823→            await new Promise((r) => setTimeout(r, 300));
   824→   824→
   825→   825→            // Evaluate design quality via API
   826→   826→            setGenerationProgressRef.current({
   827→   827→              stage: 'evaluating',
   828→   828→              stageLabel: 'Evaluating quality...',
   829→   829→              percentage: 90,
   830→   830→              message: 'Checking design quality...',
   831→   831→            });
   832→   832→
   833→   833→            try {
   834→   834→              const evalRes = await fetch('/api/design/evaluate', {
   835→   835→                method: 'POST',
   836→   836→                headers: { 'Content-Type': 'application/json' },
   837→   837→                body: JSON.stringify({ designTree: data.design }),
   838→   838→              });
   839→   839→              if (evalRes.ok) {
   840→   840→                const qualityResult = await evalRes.json();
   841→   841→                setQualityReportRef.current(qualityResult);
   842→   842→              } else {
   843→   843→                // Fallback to client-side evaluation
   844→   844→                const clientReport = evaluateDesignQuality(data.design);
   845→   845→                setQualityReportRef.current(clientReport);
   846→   846→              }
   847→   847→            } catch {
   848→   848→              // Fallback to client-side evaluation
   849→   849→              const clientReport = evaluateDesignQuality(data.design);
   850→   850→              setQualityReportRef.current(clientReport);
   851→   851→            }
   852→   852→          }
   853→   853→
   854→   854→          // Show parse failure info if AI output couldn't be parsed (smart fallback should prevent this, but just in case)
   855→   855→          if (!data.design && data.parseFailed) {
   856→   856→            const parseInfoMessage: ChatMessage = {
   857→   857→              id: `parse-info-${Date.now()}`,
   858→   858→              projectId: currentProjectId,
   859→   859→              role: 'system',
   860→   860→              content: `I had trouble generating your design. Please try describing what you'd like again!`,
   861→   861→              createdAt: new Date(),
   862→   862→            };
   863→   863→            addChatMessageRef.current(parseInfoMessage);
   864→   864→          }
   865→   865→
   866→   866→          // Stop progress with success
   867→   867→          stopProgressSimulation(true);
   868→   868→        } else {
   869→   869→          const errorData = await res.json().catch(() => ({}));
   870→   870→          const errorMessage: ChatMessage = {
   871→   871→            id: `error-${Date.now()}`,
   872→   872→            projectId: currentProjectId,
   873→   873→            role: 'system',
   874→   874→            content: `${tRef.current.common.error}: ${errorData.error || 'Unknown error'}`,
   875→   875→            createdAt: new Date(),
   876→   876→          };
   877→   877→          addChatMessageRef.current(errorMessage);
   878→   878→          stopProgressSimulation(false);
   879→   879→        }
   880→   880→      } catch {
   881→   881→        const errorMessage: ChatMessage = {
   882→   882→          id: `error-${Date.now()}`,
   883→   883→          projectId: currentProjectId,
   884→   884→          role: 'system',
   885→   885→          content: tRef.current.common.error,
   886→   886→          createdAt: new Date(),
   887→   887→        };
   888→   888→        addChatMessageRef.current(errorMessage);
   889→   889→        stopProgressSimulation(false);
   890→   890→      } finally {
   891→   891→        setIsGeneratingRef.current(false);
   892→   892→      }
   893→   893→    },
   894→   894→    [startProgressSimulation, stopProgressSimulation, evaluateDesignQuality]
   895→   895→  );
   896→   896→
   897→   897→  // Voice input handlers
   898→   898→  const handleVoiceTranscript = useCallback(
   899→   899→    (transcript: string) => {
   900→   900→      if (transcript.trim()) {
   901→   901→        sendMessage(transcript.trim());
   902→   902→      }
   903→   903→    },
   904→   904→    [sendMessage]
   905→   905→  );
   906→   906→
   907→   907→  const handleVoiceInterim = useCallback((interim: string) => {
   908→   908→    setInput((prev) => {
   909→   909→      const base = prev.replace(/🎤.*$/, '');
   910→   910→      return interim ? `${base}🎤 ${interim}` : base;
   911→   911→    });
   912→   912→  }, []);
   913→   913→
   914→   914→  const {
   915→   915→    isListening,
   916→   916→    isSupported: voiceSupported,
   917→   917→    startListening,
   918→   918→    stopListening,
   919→   919→    error: voiceError,
   920→   920→  } = useVoiceInput({
   921→   921→    onTranscript: handleVoiceTranscript,
   922→   922→    onInterimTranscript: handleVoiceInterim,
   923→   923→  });
   924→   924→
   925→   925→  // Auto-scroll to bottom
   926→   926→  useEffect(() => {
   927→   927→    if (scrollRef.current) {
   928→   928→      scrollRef.current.scrollTop = scrollRef.current.scrollHeight;
   929→   929→    }
   930→   930→  }, [chatMessages, isGenerating]);
   931→   931→
   932→   932→  const handleKeyDown = useCallback(
   933→   933→    (e: React.KeyboardEvent) => {
   934→   934→      if (e.key === 'Enter' && !e.shiftKey) {
   935→   935→        e.preventDefault();
   936→   936→        const cleanInput = input.replace(/🎤/g, '');
   937→   937→        sendMessage(cleanInput);
   938→   938→      }
   939→   939→    },
   940→   940→    [input, sendMessage]
   941→   941→  );
   942→   942→
   943→   943→  const handleExampleClick = useCallback(
   944→   944→    (prompt: string) => {
   945→   945→      sendMessage(prompt);
   946→   946→    },
   947→   947→    [sendMessage]
   948→   948→  );
   949→   949→
   950→   950→  const handleClear = useCallback(() => {
   951→   951→    clearChat();
   952→   952→  }, [clearChat]);
   953→   953→
   954→   954→  const handleVoiceToggle = useCallback(() => {
   955→   955→    if (isListening) {
   956→   956→      stopListening();
   957→   957→    } else {
   958→   958→      startListening();
   959→   959→    }
   960→   960→  }, [isListening, startListening, stopListening]);
   961→   961→
   962→   962→  const hasMessages = chatMessages.length > 0;
   963→   963→  const showProgress = isGenerating && generationProgress.stage !== 'idle';
   964→   964→
   965→   965→  return (
   966→   966→    <div className="flex flex-col h-full bg-background">
   967→   967→      {/* Header */}
   968→   968→      <div className="flex items-center justify-between px-4 h-11 border-b shrink-0">
   969→   969→        <div className="flex items-center gap-2">
   970→   970→          <Sparkles className="size-4 text-emerald-600" />
   971→   971→          <span className="text-sm font-medium">{t.chat.title}</span>
   972→   972→          {isGenerating && (
   973→   973→            <Badge
   974→   974→              variant="secondary"
   975→   975→              className="text-[10px] px-1.5 py-0 bg-emerald-100 text-emerald-700 dark:bg-emerald-900/30 dark:text-emerald-400"
   976→   976→            >
   977→   977→              {t.chat.thinking}
   978→   978→            </Badge>
   979→   979→          )}
   980→   980→        </div>
   981→   981→        {hasMessages && (
   982→   982→          <Tooltip>
   983→   983→            <TooltipTrigger asChild>
   984→   984→              <Button
   985→   985→                variant="ghost"
   986→   986→                size="icon"
   987→   987→                className="size-7"
   988→   988→                onClick={handleClear}
   989→   989→              >
   990→   990→                <Trash2 className="size-3.5 text-muted-foreground" />
   991→   991→              </Button>
   992→   992→            </TooltipTrigger>
   993→   993→            <TooltipContent side="bottom">{t.chat.clearChat}</TooltipContent>
   994→   994→          </Tooltip>
   995→   995→        )}
   996→   996→      </div>
   997→   997→
   998→   998→      {/* Messages or Welcome */}
   999→   999→      {!hasMessages ? (
  1000→  1000→        <WelcomeState onExampleClick={handleExampleClick} />
  1001→  1001→      ) : (
  1002→  1002→        <ScrollArea className="flex-1">
  1003→  1003→          <div ref={scrollRef} className="py-2 space-y-1">
  1004→  1004→            <AnimatePresence mode="popLayout">
  1005→  1005→              {chatMessages.map((message) => (
  1006→  1006→                <MessageBubble key={message.id} message={message} qualityReport={qualityReport} onEnhance={handleEnhance} isEnhancing={isEnhancing} />
  1007→  1007→              ))}
  1008→  1008→            </AnimatePresence>
  1009→  1009→            {isGenerating && !showProgress && <TypingIndicator />}
  1010→  1010→          </div>
  1011→  1011→        </ScrollArea>
  1012→  1012→      )}
  1013→  1013→
  1014→  1014→      {/* Generation Step Progress */}
  1015→  1015→      {showProgress && <GenerationStepProgress />}
  1016→  1016→
  1017→  1017→      <Separator />
  1018→  1018→
  1019→  1019→      {/* Input area */}
  1020→  1020→      <div className="p-3 shrink-0">
  1021→  1021→        <div className="relative flex items-end gap-2 bg-muted/50 rounded-xl border focus-within:border-emerald-500/50 focus-within:ring-2 focus-within:ring-emerald-500/20 transition-all">
  1022→  1022→          {/* Attachment button */}
  1023→  1023→          <Tooltip>
  1024→  1024→            <TooltipTrigger asChild>
  1025→  1025→              <Button
  1026→  1026→                variant="ghost"
  1027→  1027→                size="icon"
  1028→  1028→                className="size-8 ml-2 mb-1.5 shrink-0 text-muted-foreground hover:text-foreground"
  1029→  1029→              >
  1030→  1030→                <Paperclip className="size-4" />
  1031→  1031→              </Button>
  1032→  1032→            </TooltipTrigger>
  1033→  1033→            <TooltipContent side="top">Attach file</TooltipContent>
  1034→  1034→          </Tooltip>
  1035→  1035→
  1036→  1036→          {/* Research button */}
  1037→  1037→          <Tooltip>
  1038→  1038→            <TooltipTrigger asChild>
  1039→  1039→              <Button
  1040→  1040→                variant="ghost"
  1041→  1041→                size="icon"
  1042→  1042→                className="size-8 mb-1.5 shrink-0 text-muted-foreground hover:text-emerald-600"
  1043→  1043→                onClick={() => setResearchOpen(true)}
  1044→  1044→              >
  1045→  1045→                <Search className="size-4" />
  1046→  1046→              </Button>
  1047→  1047→            </TooltipTrigger>
  1048→  1048→            <TooltipContent side="top">
  1049→  1049→              {t.research?.button || 'Research'}
  1050→  1050→            </TooltipContent>
  1051→  1051→          </Tooltip>
  1052→  1052→
  1053→  1053→          {/* AI Image button */}
  1054→  1054→          <Tooltip>
  1055→  1055→            <TooltipTrigger asChild>
  1056→  1056→              <Button
  1057→  1057→                variant="ghost"
  1058→  1058→                size="icon"
  1059→  1059→                className="size-8 mb-1.5 shrink-0 text-muted-foreground hover:text-emerald-600"
  1060→  1060→                onClick={() => setAiImageOpen(true)}
  1061→  1061→              >
  1062→  1062→                <ImageIcon className="size-4" />
  1063→  1063→              </Button>
  1064→  1064→            </TooltipTrigger>
  1065→  1065→            <TooltipContent side="top">
  1066→  1066→              {t.aiImage?.buttonText || 'AI Image'}
  1067→  1067→            </TooltipContent>
  1068→  1068→          </Tooltip>
  1069→  1069→
  1070→  1070→          <Textarea
  1071→  1071→            ref={textareaRef}
  1072→  1072→            value={input}
  1073→  1073→            onChange={(e) => setInput(e.target.value)}
  1074→  1074→            onKeyDown={handleKeyDown}
  1075→  1075→            placeholder={
  1076→  1076→              isListening
  1077→  1077→                ? t.voice?.listening || 'Listening...'
  1078→  1078→                : t.chat.placeholder
  1079→  1079→            }
  1080→  1080→            className={`min-h-[44px] max-h-[120px] resize-none border-0 bg-transparent focus-visible:ring-0 focus-visible:border-0 shadow-none text-sm py-2.5 px-0 placeholder:text-muted-foreground/60 ${
  1081→  1081→              isListening ? 'placeholder:text-red-500/60' : ''
  1082→  1082→            }`}
  1083→  1083→            rows={1}
  1084→  1084→            disabled={isGenerating}
  1085→  1085→          />
  1086→  1086→
  1087→  1087→          <div className="flex items-center gap-0.5 mr-1.5 mb-1.5 shrink-0">
  1088→  1088→            {/* Voice input button */}
  1089→  1089→            {voiceSupported && (
  1090→  1090→              <Tooltip>
  1091→  1091→                <TooltipTrigger asChild>
  1092→  1092→                  <Button
  1093→  1093→                    variant="ghost"
  1094→  1094→                    size="icon"
  1095→  1095→                    className={`size-8 ${
  1096→  1096→                      isListening
  1097→  1097→                        ? 'text-red-500 hover:text-red-600 bg-red-50 dark:bg-red-900/20'
  1098→  1098→                        : 'text-muted-foreground hover:text-foreground'
  1099→  1099→                    }`}
  1100→  1100→                    onClick={handleVoiceToggle}
  1101→  1101→                  >
  1102→  1102→                    {isListening ? (
  1103→  1103→                      <motion.div
  1104→  1104→                        animate={{ scale: [1, 1.2, 1] }}
  1105→  1105→                        transition={{ duration: 1, repeat: Infinity }}
  1106→  1106→                      >
  1107→  1107→                        <Mic className="size-4" />
  1108→  1108→                      </motion.div>
  1109→  1109→                    ) : (
  1110→  1110→                      <Mic className="size-4" />
  1111→  1111→                    )}
  1112→  1112→                  </Button>
  1113→  1113→                </TooltipTrigger>
  1114→  1114→                <TooltipContent side="top">
  1115→  1115→                  {isListening
  1116→  1116→                    ? t.voice?.stopListening || 'Stop listening'
  1117→  1117→                    : t.voice?.startListening || 'Start voice input'}
  1118→  1118→                </TooltipContent>
  1119→  1119→              </Tooltip>
  1120→  1120→            )}
  1121→  1121→
  1122→  1122→            {/* Send button */}
  1123→  1123→            <Tooltip>
  1124→  1124→              <TooltipTrigger asChild>
  1125→  1125→                <Button
  1126→  1126→                  size="icon"
  1127→  1127→                  className="size-8 rounded-lg bg-emerald-600 hover:bg-emerald-700 text-white"
  1128→  1128→                  onClick={() => {
  1129→  1129→                    const cleanInput = input.replace(/🎤/g, '');
  1130→  1130→                    sendMessage(cleanInput);
  1131→  1131→                  }}
  1132→  1132→                  disabled={!input.replace(/🎤/g, '').trim() || isGenerating}
  1133→  1133→                >
  1134→  1134→                  <Send className="size-4" />
  1135→  1135→                </Button>
  1136→  1136→              </TooltipTrigger>
  1137→  1137→              <TooltipContent side="top">
  1138→  1138→                {t.chat.send} (Enter)
  1139→  1139→              </TooltipContent>
  1140→  1140→            </Tooltip>
  1141→  1141→          </div>
  1142→  1142→        </div>
  1143→  1143→
  1144→  1144→        {/* Voice error */}
  1145→  1145→        {voiceError && (
  1146→  1146→          <p className="text-[10px] text-red-500 mt-1 text-center">
  1147→  1147→            {voiceError}
  1148→  1148→          </p>
  1149→  1149→        )}
  1150→  1150→
  1151→  1151→        {/* Listening indicator */}
  1152→  1152→        {isListening && (
  1153→  1153→          <div className="flex items-center justify-center gap-2 mt-1.5">
  1154→  1154→            <motion.div
  1155→  1155→              className="size-2 rounded-full bg-red-500"
  1156→  1156→              animate={{ opacity: [1, 0.3, 1] }}
  1157→  1157→              transition={{ duration: 1.2, repeat: Infinity }}
  1158→  1158→            />
  1159→  1159→            <span className="text-[10px] text-red-500 font-medium">
  1160→  1160→              {t.voice?.listening || 'Listening...'}
  1161→  1161→            </span>
  1162→  1162→          </div>
  1163→  1163→        )}
  1164→  1164→
  1165→  1165→        {!isListening && (
  1166→  1166→          <p className="text-[10px] text-muted-foreground/50 mt-1.5 text-center">
  1167→  1167→            Shift+Enter for new line
  1168→  1168→          </p>
  1169→  1169→        )}
  1170→  1170→      </div>
  1171→  1171→
  1172→  1172→      {/* AI Image Dialog */}
  1173→  1173→      <AIImageDialog open={aiImageOpen} onOpenChange={setAiImageOpen} />
  1174→  1174→
  1175→  1175→      {/* Research Dialog */}
  1176→  1176→      <ResearchDialog
  1177→  1177→        open={researchOpen}
  1178→  1178→        onOpenChange={setResearchOpen}
  1179→  1179→        onResults={(query, results) => {
  1180→  1180→          // Add a user message showing what was searched
  1181→  1181→          const userMessage: ChatMessage = {
  1182→  1182→            id: `user-research-${Date.now()}`,
  1183→  1183→            projectId: projectId || '',
  1184→  1184→            role: 'user',
  1185→  1185→            content: `🔍 ${t.research?.button || 'Research'}: ${query}`,
  1186→  1186→            createdAt: new Date(),
  1187→  1187→          };
  1188→  1188→          addChatMessage(userMessage);
  1189→  1189→
  1190→  1190→          // Add an assistant message with the results
  1191→  1191→          const resultLines = results
  1192→  1192→            .map(
  1193→  1193→              (r, i) =>
  1194→  1194→                `${i + 1}. **[${r.title}](${r.url})**\n   ${r.description}\n   _${r.domain}${r.date ? ` · ${r.date}` : ''}_`
  1195→  1195→            )
  1196→  1196→            .join('\n\n');
  1197→  1197→
  1198→  1198→          const assistantMessage: ChatMessage = {
  1199→  1199→            id: `ai-research-${Date.now()}`,
  1200→  1200→            projectId: projectId || '',
  1201→  1201→            role: 'assistant',
  1202→  1202→            content: `${t.research?.results || 'Found inspiration!'} Here's what I found for "${query}":\n\n${resultLines}`,
  1203→  1203→            createdAt: new Date(),
  1204→  1204→          };
  1205→  1205→          addChatMessage(assistantMessage);
  1206→  1206→        }}
  1207→  1207→      />
  1208→  1208→    </div>
  1209→  1209→  );
  1210→  1210→}
  1211→  1211→